/**
 * i18n keys owned EXCLUSIVELY by the auth-face-fn work. One file per author — `t()` is
 * typed on `keyof typeof en`, so concurrent appends to en.ts silently lose keys.
 */
export const keysAuthFaceFn = {} as const;
