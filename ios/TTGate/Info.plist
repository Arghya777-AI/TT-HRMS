<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CFBundleDevelopmentRegion</key>
  <string>en</string>
  <key>CFBundleDisplayName</key>
  <string>TT Gate</string>
  <key>CFBundleExecutable</key>
  <string>$(EXECUTABLE_NAME)</string>
  <key>CFBundleIdentifier</key>
  <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
  <key>CFBundleInfoDictionaryVersion</key>
  <string>6.0</string>
  <key>CFBundleName</key>
  <string>$(PRODUCT_NAME)</string>
  <key>CFBundlePackageType</key>
  <string>APPL</string>
  <key>CFBundleShortVersionString</key>
  <string>1.0.0</string>
  <key>CFBundleVersion</key>
  <string>1</string>

  <!-- Shown in the permission prompt. iOS REFUSES to launch a build that touches the
       camera without this string, and the wording is what the person at the gate reads. -->
  <key>NSCameraUsageDescription</key>
  <string>The gate terminal uses the camera to recognise employees and record their attendance. Images are used only for that check.</string>

  <!-- The gate is wall-mounted and never rotates in service, but it is held in a hand while
       being installed, and a locked orientation there reads as a broken app. -->
  <key>UISupportedInterfaceOrientations</key>
  <array>
    <string>UIInterfaceOrientationPortrait</string>
    <string>UIInterfaceOrientationLandscapeLeft</string>
    <string>UIInterfaceOrientationLandscapeRight</string>
  </array>
  <key>UISupportedInterfaceOrientations~ipad</key>
  <array>
    <string>UIInterfaceOrientationPortrait</string>
    <string>UIInterfaceOrientationPortraitUpsideDown</string>
    <string>UIInterfaceOrientationLandscapeLeft</string>
    <string>UIInterfaceOrientationLandscapeRight</string>
  </array>

  <key>UIStatusBarHidden</key>
  <true/>
  <key>UIViewControllerBasedStatusBarAppearance</key>
  <true/>
  <key>UILaunchStoryboardName</key>
  <string>LaunchScreen</string>

  <!-- Which deployment this build points at. Left empty so the default in
       GateViewController.gateURL applies; set it to target a preview deployment. -->
  <key>TTGateURL</key>
  <string></string>

  <!-- No arbitrary-load exception. The gate is HTTPS only, and Web Crypto (which signs every
       punch) refuses to work outside a secure context anyway — so allowing plain HTTP would
       buy a connection that could not sign a request. -->
  <key>NSAppTransportSecurity</key>
  <dict>
    <key>NSAllowsArbitraryLoads</key>
    <false/>
  </dict>
</dict>
</plist>
